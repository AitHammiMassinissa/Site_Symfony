{% extends 'Layouts/base.html.twig'%}
{% block title 'Modifier La formation'~ description.getId() %}
{% block body %}

    <div class="row mb-5">
        <div class="mt-3 mb-3 col-md-6 mx-auto">
            <h1>Modifier La formation  {{description.getNomFormation()}}</h1>
            {{ include('formations/_form.html.twig',{submitButtonText:'Modifier la formation'}) }}
        </div>
    </div>
{% endblock %}
